# scoop-daslang

The Scoop bucket for the daslang SDK.

```
scoop bucket add daslang https://github.com/borisbat/scoop-daslang
scoop install daslang
```

Manifest of record: the daslang repo ci/packaging/ templates; per-release ritual there.
